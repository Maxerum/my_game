package application;

public class Saving {
	
	public void saveGame(int index) {

		int offset;
		/*
		 * if ((offset = getFilePos(index)) != -1) { try { RandomAccessFile file = new
		 * RandomAccessFile(fileName, "rw");
		 * 
		 * file.seek(offset);
		 * 
		 * file.writeInt((2 + fieldSize * fieldSize) * 4); file.writeInt(fieldSize);
		 * file.writeInt(currentScore);
		 * 
		 * for (int i = 0; i < fieldSize; i++) { for (int j = 0; j < fieldSize; j++) {
		 * 
		 * file.writeInt(copyBoard[i][j]); } } file.close();
		 * 
		 * } catch (IOException e) { e.printStackTrace(); } }
		 */
	}
}